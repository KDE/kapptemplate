echo "Creating $LOCATION_ROOT/src...";
$MKDIR $LOCATION_ROOT/src
echo "Creating $LOCATION_ROOT/icons...";
$MKDIR $LOCATION_ROOT/icons
echo "Creating $LOCATION_ROOT/doc...";
$MKDIR $LOCATION_ROOT/doc/
echo "Creating $LOCATION_ROOT/CMakeLists.txt...";
cat << EOF > $LOCATION_ROOT/CMakeLists.txt

PROJECT(${APP_NAME_LC})
find_package(KDE4 REQUIRED)
include (KDE4Defaults)
include_directories( \${KDE4_INCLUDES} \${QT_INCLUDES} )

add_subdirectory( src )

add_subdirectory( icons )
add_subdirectory( doc )

